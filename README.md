# Module 3 — Zepto Support Assistant (`/support_assistant`)

An offline-first GenAI retrieval and routing pipeline for Zepto policies using LangGraph, ChromaDB, and FastAPI.

---

## 1. RAG Pipeline Architecture Description

The pipeline processes user queries through four distinct stages:
